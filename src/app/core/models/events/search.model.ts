export interface TabToggle {
  key: string;
  label: string;
  value: boolean | null;
}

export interface SearchLocation {
  name: string;
  id: string;
}

export interface PopularLocation {
  name: string;
  meta: string;
}
